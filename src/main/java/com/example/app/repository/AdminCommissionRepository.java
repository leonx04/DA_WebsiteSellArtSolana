package com.example.app.repository;

import com.example.app.model.AdminCommission;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AdminCommissionRepository extends JpaRepository<AdminCommission, Long> {
}
